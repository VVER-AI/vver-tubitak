@AGENTS.md

## Workflow

Commit and push changes to GitHub automatically as part of normal iteration on this
project (no need to ask before each push). Vercel is connected to this GitHub repo for
auto-deploy on push to `main`, and Supabase env vars are kept in sync in Vercel project
settings, so a push here should be enough to reflect in production.
